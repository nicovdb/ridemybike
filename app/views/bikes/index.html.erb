

<h1>BIKES</h1>


<div class="container">
  <div class="row">
  <% @bikes.each do |bike| %>
    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
      <div class="card">
        <div class="card-photo" style="background-image: url('<%= default_path_photo(bike) %>');">
        </div>
        <div class="card-infos">
          <h2><%= bike.title %></h2>
          <h3><%= bike.category %></h3>
          <p><%= bike.description %></p>
        </div>
      </div>
    </div>
  <% end %>
  </div>
</div>


